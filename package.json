{
  "name": "flag-safe",
  "version": "0.0.1",
  "description": "Typesafe feature flags for NextJS and React",
  "main": "index.js",
  "workspaces": [
    "flagsafe",
    "examples/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "publish-packages": "turbo run build && changeset version && changeset publish"
  },
  "repository": "https://github.com/drew-harris/flag-safe",
  "author": "Drew Harris",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@changesets/cli": "^2.26.1",
    "turbo": "^1.8.5"
  }
}
